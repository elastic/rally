[meta]
type=mixin
description=Verbose Logging

# these config bases are reverse by intention. As we should use this as a mixin (in tests) we want to see whether we
# actually preserve order of the "vanilla" config base when overriding.
[config]
base=verbose_logging,vanilla
